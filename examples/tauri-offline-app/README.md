# Tauri Offline App Example

This shows how to invoke `yes-human` routing commands via Tauri Command bindings.

You can run the Node SDK inside a local sidecar or compile a TypeScript backend interface communicating via localhost IPC.

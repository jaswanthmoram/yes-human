name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  gate:
    name: Gate (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 22
          - 24

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Node dependencies
        run: npm ci

      - name: Install Python dependencies
        run: python -m pip install -r requirements.txt

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format:check

      - name: Detect stub skills
        run: npm run detect:skill-stubs

      - name: Validate schemas and registries
        run: npm run validate

      - name: Audit dossiers strict
        run: npm run audit:dossiers:strict

      - name: Audit production promotion
        run: npm run audit:promotion

      - name: Audit production skills
        run: npm run audit:skills
      - name: Route drift
        run: npm run validate:drift

      - name: Run tests
        run: npm test

      - name: Code coverage
        run: npm run test:coverage

      - name: Evaluate routing
        run: node packages/yes-cli/index.js eval route

      - name: Evaluate boot token cost
        run: npm run eval:cost

      - name: Doctor
        run: npm run doctor

      - name: Enforce core routing performance baseline
        run: npm run bench:assert

      - name: Build all host bundles
        run: node packages/yes-cli/index.js build all

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITLEAKS_ENABLE_COMMENTS: false
        with:
          config-path: .gitleaks.toml

# @yes-human/graph

Provides indexing and search features to support context resolution and semantic-containment routing.

## Capabilities

* **`trie.js`**: Longest-match Word Trie search used for phrase mapping in stage 3 keyword trie routing.
* **`code-graph.js`**: SQLite index manager scanning workspace symbols, imports, and directories.
